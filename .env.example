# Strapi API access (read-only lookups in Phase 1; will need write scope in Phase 2)
STRAPI_API_URL=https://aikyamjobs.org/api
STRAPI_API_TOKEN=

# Local server port
PORT=4100

# Path to the claude CLI binary. Usually just "claude" if it's on PATH.
CLAUDE_BIN=claude
